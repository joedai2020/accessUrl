name: Wake Up Web App

on:
  schedule:
    # 前一天的 UTC 21:00–23:45（对应 JST 6:00–8:45）
    - cron: '0,15,30,45 21-23 * * *'
    # 当天的 UTC 0:00–13:45（对应 JST 9:00–22:45）
    - cron: '0,15,30,45 0-13 * * *'

jobs:
  wakeup:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to Web App
        run: curl -s https://bookingbm-dnbscacuavc7dhbb.eastasia-01.azurewebsites.net/login/
